# Server config
PORT=3000
HOST=0.0.0.0

# API key for clients (change this to a random string)
API_KEY=change-me-to-a-random-secret

# SQLite database file path (relative to server/)
DATABASE_PATH=./heartbeat.db
