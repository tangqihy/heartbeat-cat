services:
  heartbeat:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - heartbeat-data:/data
    environment:
      - API_KEY=${API_KEY:-change-me-to-a-random-secret}
      - DATABASE_PATH=/data/heartbeat.db
    restart: unless-stopped

volumes:
  heartbeat-data:
